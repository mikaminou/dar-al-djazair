/**
 * app-params.js — retained as an empty stub for backward compatibility.
 * All base44-specific parameters (appId, token, etc.) have been removed.
 * Auth is now handled entirely by Supabase (see src/lib/AuthContext.jsx).
 */
export const appParams = {};

